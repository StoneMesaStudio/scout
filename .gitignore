# macOS
.DS_Store

# Xcode / build
DerivedData/
/build/
xcuserdata/
*.xcuserstate

# Generated by XcodeGen from project.yml (reproducible — not committed)
*.xcodeproj

# Swift Package Manager
.swiftpm/
.build/
Core/.build/
Package.resolved

# Local Claude settings
.claude/settings.local.json

# Release artefacts — bin/release.sh writes the notarised DMG here. The shipped copy lives
# in the website repo, not this one.
dist/
